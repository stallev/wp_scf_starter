---
status: planned
version: 0.1
updated: 2026-09-25
---

# Страница `/<slug>/` — <название>

**Приоритет:** <1–4 по [`sitemap.md`](../sitemap.md)> · **Шаблон:** `<page-slug.php>` · **Прототип:** `prototype/<file>.html` · **Запись:** `pages-map.json` → `url: /<slug>/`

Копировать в `pages/<slug>.md`, путь добавить в `specs` записи `pages-map`. Перед вёрсткой — прогнать [`_checklist.md`](_checklist.md).

## Search intent

<Одно намерение. Чем страница отличается от смежных (заголовок / оффер / аудитория), и какая страница отвечает за общие данные, а какая только ссылается.>

## SEO-параметры

| Параметр | Значение |
|---|---|
| URL | `/<slug>/` |
| Title | <по [`meta-patterns.md`](../seo/meta-patterns.md)> |
| Meta description | |
| H1 | |
| Schema | <Service / FAQPage / …> + BreadcrumbList (Yoast) |
| Целевые кластеры (частоты, источник) | |
| Второстепенно (в тексте, не в заголовках) | |
| Не использовать | <слова / топонимы, исключённые стратегией> |

## Первый экран (для `pages-map`)

- LCP: `<selector>` (`text` / `image`) · `above_fold`: `<selectors>` — без `.reveal`.
- Форма: `lead_form: true/false`; ранние CTA — карточка звонка со ссылкой на форму.

## Блоки

### 1. Крошки + H1 + подзаголовок
<суть; если есть фиксированная цена — сразу, без «от»>

### 2. <Блок>
<Содержание, формат (таблица / список / карточки), источник данных: вёрстка / поле / CPT.>

### N. Форма заявки (единственная на странице)

## Перелинковка

- Исходящие: <URL и повод>
- Входящие: <с каких страниц и блоков; минимум с главной>

## Осознанно не включено

<что и почему>

## Что нужно от заказчика

1. <цифры, фото, ответы на FAQ>

## Антипаттерн

<чего на этой странице быть не должно — например, пересказа техданных другой страницы>
